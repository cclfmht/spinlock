[package]
name = "spinlock"
version = "0.1.0"
edition = "2024"

[dependencies]
core_affinity = "0.8.3"

[dev-dependencies]
criterion = { version = "0.8.2", features = ["html_reports"] }

[[bench]]
name = "bench_mcs"
harness = false
